language: r

matrix:
  include:
    - r: oldrel
      os: linux
      sudo: false
    - r: release
      os: linux
      sudo: false
      env: 
        - TEST_GOLUB=TRUE
    - r: devel
      os: linux
      sudo: false
    - r: release
      os: osx
      env:
        - TEST_GOLUB=TRUE

